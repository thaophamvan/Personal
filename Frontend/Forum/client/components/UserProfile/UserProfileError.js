import React from 'react';

const propTypes = {
};

const defaultProps = {
};

const UserProfileError = () => (
  <div className="bn_profile__notification">
    Det här användaren blockerades
  </div>
);

UserProfileError.propTypes = propTypes;
UserProfileError.defaultProps = defaultProps;

export default UserProfileError;
