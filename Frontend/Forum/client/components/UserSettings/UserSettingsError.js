import React from 'react';

const propTypes = {
};

const defaultProps = {
};

const UserSettingsError = () => (
  <div className="bn_user-settings__notification">
    Det här kontot har loggats ut
  </div>
);

UserSettingsError.propTypes = propTypes;
UserSettingsError.defaultProps = defaultProps;

export default UserSettingsError;
